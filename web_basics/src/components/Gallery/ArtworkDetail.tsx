import React from 'react';

const ArtworkDetail: React.FC = () => {
  return (
    <div className="artwork-detail">
      {/* TODO: Display full image and metadata (title, description, media, etc.) */}
    </div>
  );
};

export default ArtworkDetail;